$('.concertContainerTest').html("<%= j(render partial: 'concert_container_venues', collection: @concerts, :as => 'concert',locals: {keep: @keep}) %>");
$('.post_button').removeClass('activeBtn');
$('#sortVenue').addClass('activeBtn');