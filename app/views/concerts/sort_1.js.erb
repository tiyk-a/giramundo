$('.concertContainerTest').html("<%= j(render partial: 'concert_container', collection: @concerts, :as => 'concert',locals: {keep: @keep}) %>");
$('.post_button').removeClass('activeBtn');
$('#sortAsc').addClass('activeBtn');